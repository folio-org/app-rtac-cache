{
  "name": "${project.name}",
  "version": "${project.version}",
  "description": "${project.description}",
  "platform": "complete",
  "dependencies": [
    {
      "name": "app-rtac",
      "version": "^VERSION_^1.0.0",
      "preRelease": "false"
    }
  ],
  "modules": [
    {
      "name": "mod-rtac-cache",
      "version": "^VERSION_latest",
      "preRelease": "false"
    }
  ]
}
